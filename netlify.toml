[build]
  command = "npm run build"
  publish = ".next"

[[plugins]]
  package = "@netlify/plugin-nextjs"

[[headers]]
  for = "/*"
  [headers.values]
    X-Frame-Options = "DENY"
    X-Content-Type-Options = "nosniff"
    X-XSS-Protection = "1; mode=block"
    Referrer-Policy = "no-referrer-when-downgrade"

# Ensure all traffic uses HTTPS and your custom domain (canonical redirect)
# [[redirects]]
#   from = "https://michael-itindi.netlify.app/*"
#   to = "https://itindi.com/:splat"
#   status = 301
#   force = true
